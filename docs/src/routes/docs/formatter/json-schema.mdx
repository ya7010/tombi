# Json Schema

Tombi provides an option to automatically sort the keys of tables by reading the `x-tombi-*` defined in JSON Schema.

For more details, please refer to [JSON Schema](/docs/json-schema#formatting).
